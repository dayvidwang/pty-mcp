{
  "name": "pty-mcp",
  "version": "0.1.0",
  "type": "module",
  "description": "MCP server for headless terminal emulation — spawn PTY sessions, send keystrokes, capture screenshots",
  "license": "MIT",
  "keywords": ["mcp", "terminal", "pty", "headless", "xterm", "model-context-protocol"],
  "bin": {
    "pty-mcp": "src/server.ts"
  },
  "files": ["src/server.ts", "src/terminal.ts", "src/renderer.ts", "src/pty.ts", "src/runtime.ts", "assets/", "README.md"],
  "scripts": {
    "start": "bun src/server.ts",
    "start:node": "node --import tsx src/server.ts",
    "dev": "bun --watch src/server.ts",
    "test": "vitest run",
    "test:bun": "bun vitest run",
    "test:node": "node --import tsx node_modules/.bin/vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "@xterm/headless": "^6.0.0",
    "@xterm/addon-serialize": "^0.14.0",
    "@napi-rs/canvas": "^0.1.94",
    "zod": "^3.25.0"
  },
  "optionalDependencies": {
    "bun-pty": "^0.4.8",
    "node-pty": "^1.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "tsx": "^4.19.0",
    "vitest": "^3.0.0"
  }
}
